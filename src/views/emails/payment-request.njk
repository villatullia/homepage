{% extends "_layout.njk" %}{% block content %}<h1 style="font:26px Georgia,serif">{% if isBalance %}Remaining balance{% else %}Secure payment{% endif %}</h1><p>Hello {{ guestName }}, {% if isBalance %}the remaining balance for your confirmed stay is{% else %}both parties have signed your agreement. The amount currently due is{% endif %} <strong>{{ amount }}</strong>, payable by {{ deadline }}.</p><p><a href="{{ guestUrl }}" style="display:inline-block;padding:13px 18px;background:#b66b4f;color:white;text-decoration:none">Pay securely</a></p><p style="font-size:12px;color:#687671">For your safety, use only the secure button above. Villa Tullia never receives or stores your card details.</p>{% endblock %}
