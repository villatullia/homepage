{% extends "_layout.njk" %}{% block content %}<h1 style="font:26px Georgia,serif">{% if isBalance %}Balance reminder{% else %}Payment reminder{% endif %}</h1><p>The {{ paymentLabel }} of <strong>{{ amount }}</strong> for {{ reference }} is due by {{ deadline }}.</p><p><a href="{{ guestUrl }}" style="display:inline-block;padding:13px 18px;background:#b66b4f;color:white;text-decoration:none">Pay securely</a></p>{% endblock %}
